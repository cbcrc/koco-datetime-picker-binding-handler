{
  "name": "koco-datetime-picker-binding-handler",
  "version": "1.3.1",
  "homepage": "https://github.com/cbcrc/koco-datetime-picker-binding-handler",
  "authors": [
    "Mark Moyes <mark.moyes@cbc.ca>"
  ],
  "description": "Knockout components binding handler for Bootstrap 3 Datepicker",
  "main": "src/datetime-binding-handler.js",
  "keywords": [
    "koco",
    "knockout",
    "datetime-picker"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "license": "MIT",
  "dependencies": {
    "eonasdan-bootstrap-datetimepicker": "^4.14.30",
    "knockout": "^3.4.0",
    "moment": "^2.12.0",
    "moment-timezone": "^0.5.4",
    "jquery": "^2.1.4",
    "bootstrap": "^3.3.6"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
